name: pr-validation

on:
  pull_request:
    branches: 
    - '*'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: smoke test
      run: source ./src/nb.sh
      env:
        CI: true